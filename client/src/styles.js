import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  header: {
    margin: "45px auto",
  },
  textInput: {
    width: "100%",
  },
  clearBtn: {
    display: "block",
    margin: "auto auto",
  },
}));
